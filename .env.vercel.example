# Vercel UI-only deployment (API runs on Render — see docs/deployment.md)
NEXT_PUBLIC_BACKEND_URL=https://hallucination-tribunal.onrender.com

# Do not set NEXT_PUBLIC_API_ROUTE_PREFIX in production split deploy.
# Do not set OLLAMA_* or API_ROOT_PATH on Vercel — the Render API handles LLM/RAG.
